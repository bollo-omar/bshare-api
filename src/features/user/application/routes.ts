const baseUrl = "auth/user";

export const SIGNUP = `/${baseUrl}/signup`;
export const LOGIN = `/${baseUrl}/login`;
export const LISTALLUSERS = `/${baseUrl}/`;
export const UPDATEUSERADDRESS = `/${baseUrl}/address/:id`;
export const FINDUSERBYID = `/${baseUrl}/:id`;
export const PROFILE = `/${baseUrl}/profile`;